# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Member data. Any export of the schedule carries family names, phone numbers
# and email addresses — never committed, and never placed in public/, which
# ships verbatim to the deployed site with no authentication.
*.csv
schedule-export*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Throwaway Postgres clusters created by supabase/tests
supabase/tests/.pgdata-*

# TypeScript build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts
